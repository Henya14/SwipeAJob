<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">


    <TextView
        android:id="@+id/startEndTextView"
        android:layout_width="@dimen/recyclerview_width"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_recycler_view_items"
        tools:text="2006-2010"
        android:textSize="@dimen/recycler_view_item_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


    <TextView
        android:id="@+id/companyNameTextView"
        android:layout_width="@dimen/recyclerview_width"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_recycler_view_items"
        android:textSize="@dimen/recycler_view_item_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/startEndTextView"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Comapny Name" />

    <ImageButton
        android:id="@+id/experienceItemRemoveButton"
        style="@style/Widget.AppCompat.Button.Borderless"
        android:layout_width="@dimen/recyclerview_remove_button_width"
        android:layout_height="@dimen/recyclerview_remove_button_height"
        android:layout_margin="@dimen/app_margin"
        android:layout_gravity="center_vertical"
        android:contentDescription="@string/delete_button_description"
        android:scaleType="fitXY"
        android:src="@android:drawable/ic_menu_delete"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>